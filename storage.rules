rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Default: only authenticated users can read/write storage objects.
    // Tighten further per-object as needed.
    allow read: if request.auth != null;
    allow write: if request.auth != null;
  }
}
